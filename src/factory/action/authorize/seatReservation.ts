import * as cinerino from '@cinerino/factory';

import { IExtendId } from '../../autoGenerated';
import { IPerformanceWithDetails } from '../../performance';

export import ObjectType = cinerino.action.authorize.offer.seatReservation.ObjectType;
export import IPurpose = cinerino.action.authorize.offer.seatReservation.IPurpose;

/**
 * 承認されたオファーインターフェース
 */
export interface IAcceptedOffer {
    seat_code?: string;
    ticket_type: string;
    watcher_name: string;
}

/**
 * 座席予約承認アクション結果
 */
export type IResult = cinerino.action.authorize.offer.seatReservation.IResult<cinerino.service.webAPI.Identifier.Chevre>;

/**
 * 座席予約承認アクションオブジェクト
 */
export interface IObject {
    typeOf: cinerino.action.authorize.offer.seatReservation.ObjectType.SeatReservation;
    event: IPerformanceWithDetails;
    acceptedOffer: cinerino.chevre.event.screeningEvent.IAcceptedTicketOfferWithoutDetail[];
}

export type IError = any;

export interface IAttributes extends cinerino.action.IAttributes<cinerino.actionType.AuthorizeAction, IObject, IResult> {
    purpose: IPurpose;
}

/**
 * 座席予約承認アクションインターフェース
 */
export type IAction = IExtendId<IAttributes>;
