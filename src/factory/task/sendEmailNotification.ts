import { creativeWork } from '@cinerino/factory';

import { IExtendId } from '../autoGenerated';
import * as TaskFactory from '../task';
import TaskName from '../taskName';

export interface IData {
    /**
     * 取引ID
     * この値があれば、後で取引から参照できる
     */
    transactionId?: string;
    /**
     * Eメールメッセージ
     */
    emailMessage: creativeWork.message.email.ICreativeWork;
}

export interface IAttributes extends TaskFactory.IAttributes<TaskName.SendEmailNotification> {
    data: IData;
}

export type ITask = IExtendId<IAttributes>;
